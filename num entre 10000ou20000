#include <stdio.h>
main(){
    int N, X=0;
    int num;
    scanf("%d",&N);
    while(X<N){
        scanf("%d",&num);
        if(num>10000 && num<20000){
            printf("%d in\n",num);
        }
            else{
                printf("%d\n",num);
            }


    }
    return 0;
}
